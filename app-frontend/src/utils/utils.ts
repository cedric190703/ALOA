export enum ItemNav {
    Home = 'home',
    User = 'user',
    Patients = 'patients',
    Calendar = 'calendar',
    Create = 'create',
    Settings = 'settings'
}

export enum TriageType {
    Urgent = 'urgent',
    NonUrgent = 'nonUrgent',
    Emergency = 'emergency'
}

export enum SettingsType {
    Light = "light",
    Logout = "logout",
    Security = "security",
    AboutUs = "aboutus",
}